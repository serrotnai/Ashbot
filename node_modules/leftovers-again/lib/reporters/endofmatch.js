'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _chalk = require('chalk');

var _chalk2 = _interopRequireDefault(_chalk);

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _log = require('../log');

var _log2 = _interopRequireDefault(_log);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var winSymbol = _chalk2.default.bold.green('✓');
var loseSymbol = _chalk2.default.bold.red('X');

var EndOfMatch = function () {
  function EndOfMatch() {
    _classCallCheck(this, EndOfMatch);
  }

  _createClass(EndOfMatch, [{
    key: 'report',
    value: function report(state, path) {
      var xo = '';
      var matchup = '';
      state.forEach(function (match) {
        xo += match.won ? winSymbol : loseSymbol;
        match.myDead = match.mine.filter(function (mon) {
          return mon.dead;
        }).length;
        match.yourDead = match.yours.filter(function (mon) {
          return mon.dead;
        }).length;
        matchup += match.yourDead + '-' + match.myDead + ' ';
      });
      console.log('WINS: ' + xo);
      console.log('KOs: ' + matchup.trim());

      // write the last match to a log
      var last = state[state.length - 1];
      var out = [last.matchid, last.me, last.you, last.won, last.yourDead, last.myDead];
      out = out.join(',') + '\n';

      _log2.default.debug('checking this path:', path);
      _fs2.default.exists(path, function (exists) {
        if (!exists) {
          out = 'matchid,me,you,won,yourDead,myDead\n' + out;
        }
        // don't worry, this creates files if they don't exist
        _fs2.default.appendFile(path, out, function (err) {
          if (err) {
            _log2.default.error(err);
          }
        });
      });
    }
  }]);

  return EndOfMatch;
}();

var eom = new EndOfMatch();
exports.default = eom;