'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

var _battle = require('./battle');

var _battle2 = _interopRequireDefault(_battle);

var _log = require('./log');

var _log2 = _interopRequireDefault(_log);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

var BotClass = void 0;

var BattleManager = function () {
  function BattleManager(Bot) {
    var timeout = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 0;

    _classCallCheck(this, BattleManager);

    if (!Bot) {
      _log2.default.error('BattleManager called with no bot! That is bad.');
    }
    BotClass = Bot;
    this.battles = {};
    this.timeout = timeout;
  }

  _createClass(BattleManager, [{
    key: 'find',
    value: function find(id) {
      if (!this.battles[id]) {
        var bot = BotClass.default ? new BotClass.default() : new BotClass(); // eslint-disable-line
        this.battles[id] = new _battle2.default(id, bot, this.timeout);
      }
      return this.battles[id];
    }
  }]);

  return BattleManager;
}();

exports.default = BattleManager;